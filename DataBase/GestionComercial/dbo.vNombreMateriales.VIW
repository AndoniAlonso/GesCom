SET QUOTED_IDENTIFIER OFF 
GO
SET ANSI_NULLS ON 
GO

CREATE VIEW dbo.vNombreMateriales
AS
SELECT MATERIALID, RTRIM(CODIGO) + '-' + RTRIM(NOMBRE) 
    AS Nombre
FROM Materiales

GO
SET QUOTED_IDENTIFIER OFF 
GO
SET ANSI_NULLS ON 
GO

